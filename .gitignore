node_modules/
npm-debug.log*
.DS_Store
dist/

# Real secrets never get committed...
.env
.env.*
.npmrc

# ...but the templates with the placeholders do. Without these two negations
# `.env.*` would swallow `.env.example` and nobody would ever notice.
!.env.example
!.npmrc.example
